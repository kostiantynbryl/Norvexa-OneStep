# Security

Do not open public issues containing private user data, credentials, signing keys, or unreleased security details.

For a security report, contact the repository owner privately and include:

- affected version;
- reproduction steps;
- expected and actual behavior;
- impact assessment;
- suggested mitigation, when available.

The application must never commit signing files, service credentials, API keys, production endpoints containing secrets, or exported user backups.
